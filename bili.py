from api import *
if __name__ == '__main__':
    # main()
    # findurl()
    cv_list = []  # 初始化一个cv_list
    # test mid 573495526
    mid = '573495526'  # 一个mid
    # cv_list = findCvNumber(cv_list, mid)  # 调用findCvNumber抓去cv号列表
    # main(cv_list)  # 调用main进行对每个cv号进行爬取
    print('Hello ' + platform_name[0])  # 标志性输出
